import SwiftUI
import PhoneNumberKit

struct SitterRegistrationView: View {
    let selecteRole: String
    
    var body: some View {
        VStack {
            Text(selecteRole)
        }
        .padding()
    }
}
